package com.namanh.lovedays.features

import javax.inject.Inject

class GetMemories
@Inject constructor(private val moviesRepository: Memory) {

//    override suspend fun run(params: None) = moviesRepository.movies()
}